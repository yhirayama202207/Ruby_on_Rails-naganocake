<h5>配送先登録／一覧</h5>
</br>

<%= form_with model: @address, url: addresses_path, method: :post do |f| %>

&emsp;&emsp;
<label>郵便番号（ハイフンなし）</label>&emsp;
<%= f.text_field :postal_code %></br>

&emsp;&emsp;
<label>住所</label>&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;
<%= f.text_field :addresses %></br>

&emsp;&emsp;
<label>宛名</label>&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;
<%= f.text_field :name %>

&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;
<%= f.submit "新規登録", class: "btn btn-success" %>
<% end %>

</br>

    <div class= "col-md-8 offset-md-1">
        <table class="table table-hover table-inverse">
          <thead>
            <tr>
              <th>郵便番号</th>
              <th>住所</th>
              <th>宛名</th>
              <th></th>
              <th colspan="4"></th>
            </tr>
          </thread>
          <tbody>
              <% @addresses.each do |address| %>
              <tr>
              <td>
                <%= address.postal_code %>
              </td>
              <td>
                <%= address.address %>
              </td>
              <td>
                <%= address.name %>
              </td>
              <td>
                <%= link_to "編集する", addresses_path(@address.id), class: "btn btn-success" %>
                <%= link_to "削除する", addresses_path(@address.id), method: :delete, class: "btn btn-danger", "data-confirm" => "本当に削除しますか？" %>
              </td>

              </tr>
              <% end %>
          </tbody>
        </table>