<h2>ジャンル一覧・追加 views/admin/genres/index</h2>

<h2>ジャンル一覧・追加</h2>

<%= form_with model: @genre do |f| %>
<label>ジャンル名</label><%= f.text_field :name %></br>
<%= f.submit "新規登録", class: "btn btn-success" %></br>
<% end %>

ジャンル一覧（テーブル）
ジャンル名　編集ボタン

<div class= "col-md-8 offset-md-1">
        <table class="table table-hover table-inverse">
          <thead>
            <tr>
              <th>ジャンル</th>
              <th></th>
              <th colspan="3"></th>
            </tr>
          </thread>
          <tbody>
              <% @genres.each do |genre| %>
              <tr>
              <td><%= @genre.id.name %></td>
              <td>
                <%= link_to edit_admin_genre_path(@genre.id), "編集する", class: "btn btn-success" do %>
                <% end %>
              </td>
              </tr>
              <% end %>
          </tbody>
        </table>
      </div>
    </div>
  </div>
</main>
