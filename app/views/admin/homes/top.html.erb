<h2>管理者トップページ（注文履歴一覧）</h2>

    <div class= "col-md-8 offset-md-1">
        <h2>注文履歴一覧</h2>
        <table class="table table-hover table-inverse">
          <thead>
            <tr>
              <th>購入日時</th>
              <th>購入者</th>
              <th>注文個数</th>
              <th>注文ステータス</th>
              <th colspan="4"></th>
            </tr>
          </thread>
          <tbody>
              <% @orders.each do |order| %>
              <tr>
              <td>
                <%= link_to admin_order_path(order.id) do %>
                <%= order.created_at %>
                <% end %>
              </td>
              <td>
                <%= @customer.last_name %><%= @customer.first_name %>
              </td>
              <td>
                <%= @order_details.amount %>
              </td>
              <td>
                <%= @order.status_i18n[:status] %>
              </td>

              </tr>
              <% end %>
          </tbody>
        </table>

        <%= paginate @orders %>